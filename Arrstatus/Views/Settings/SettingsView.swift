//
//  SettingsView.swift
//  Arrstatus
//
//  Stub — configuration is now file-based (~/.config/arrstatus/arrstatus.conf)
//

import SwiftUI

struct SettingsView: View {
    var body: some View {
        Text("Edit ~/.config/arrstatus/arrstatus.conf to configure Arrstatus.")
            .padding()
            .frame(width: 400, height: 100)
    }
}
